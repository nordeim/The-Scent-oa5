{
    "require": {
        "stripe/stripe-php": "^17.1",
        "phpmailer/phpmailer": "^6.10"
    },
    "autoload": {
        "classmap": [
            "controllers",
            "models",
            "includes"
        ]
    },
    "scripts": {
        "serve": "php -S localhost:8000"
    },
    "config": {
        "optimize-autoloader": true
    }
}
